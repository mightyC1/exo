# GLM-5.2/5.3 MTP speculative decoding — recon (Фаза 0)

Статус: **ГЕЙТ 0-A закрыт**, go на side-car экстракцию → Фаза 1 (shadow).
База: ветка `mightyC1` @ `0b5fe626` (site-defaults W-серии). Пины:
mlx-lm `rltakashige @ 6a3df6cd`, mlx `mightyC1/mlx-jaccl-fix-small-recv @ cc3f3e60`.
ТЗ: glm52-mtp-tz-v1. Все file:line ниже проверены глазами по пину/дереву.

## 1. Аудит весов (§3.1) — итог

`sanitize` пина дропает `layers.N ≥ num_hidden_layers` (ds32:513–521) →
в обоих idxbf16-конвертах **0** MTP-тензоров (скан index.json). Источники:

| | GLM-5.2 (`zai-org/GLM-5.2`) | GLM-5.3 (`zai-org/GLM-5.3`) |
|---|---|---|
| формат слоя 78 | **bf16** (без scale_inv), 791 тензор / 5 шардов | **fp8 e4m3 + block-128 scale_inv**, 1569 (791+778 скейлов) / 3 шарда |
| bf16-исключения вендора | всё | eh_proj, indexer.weights_proj, k_norm, нормы, mlp.gate (`modules_to_not_convert`) |

Состав слоя 78 = полный DecoderLayer семейства + MTP-обвес:
MLA-attention (q_a/q_b/kv_a/kv_b/o + нормы) + **собственный полный индексер**
(wq_b [4096,2048] / wk [128,6144] / weights_proj [32,6144] / k_norm) +
MoE (256 routed + shared_experts + gate c e_score_correction_bias) +
`enorm/hnorm/eh_proj [6144,12288]` + `shared_head.norm`. `shared_head.head`
отсутствует → lm_head **tied** с главной моделью (как и embed) — схема ТЗ §4
подтверждена побайтно по именам. Шейпы согласуются с конфигом
(H=64, nope=192, rope=64, v=256, kv_lora=512, q_lora=2048, hidden=6144).

Конфиги: `num_nextn_predict_layers=1`, `index_share_for_mtp_iteration=true`,
`indexer_types` длиной **78** (слой 78 разметкой не покрыт). idxbf16 quant:
`{group_size:64, bits:8, mode:affine}`; per-module False-флаги индексера есть
только в 5.2-конфиге — косметика двух прогонов конверта, рантайм-поведение
presence-driven (наличие `<name>.scales`), side-load делаем так же.

**Развилка индексера закрыта:** у MTP-слоя свой полный индексер → блок
самодостаточен, top-k из главного прохода не пробрасываем;
`index_share_for_mtp_iteration` в v1 игнорируем (семантика verify-итерации
serving-движков). Vendor-тень `indexers_proj` в modules_to_not_convert —
опечатка, реальный тензор `indexer.weights_proj`.

## 2. Side-car политика (tools/glm52_mtp_extract_sidecar.py)

Та же политика, что у главного idxbf16-конверта:
1. fp8→bf16 — **дословная** математика dequant из sanitize пина
   (ds32:524–539, block-128, pad+reshape+scale_inv); 5.2 — passthrough.
2. Трансформы sanitize для слоя 78: expert-stack `experts.{e}` →
   `switch_mlp` (ds32:553–563); split `kv_b_proj` → `embed_q/unembed_out`
   (ds32:565–599).
3. int8 g64 affine: q_a/q_b/kv_a/o, embed_q/unembed_out, switch_mlp×3,
   shared_experts×3 (12 модулей).
4. bf16 навсегда: весь индексер (урок idxbf16), eh_proj и mlp.gate.weight
   (вслед за вендором; routing-чувствительно), все нормы;
   `e_score_correction_bias` — f32 (cast_predicate пина, ds32:666).
5. Выход: `mtp.safetensors` (51 тензор) + `mtp.manifest.json` (sha256,
   политика) рядом с моделью; `EXO_GLM52_MTP_WEIGHTS=auto` ищет там.

Parity-валидация (`--self-test`, реальные операторы, CPU-mlx + пин):
dequant / switch_mlp / embed_q+unembed_out **байт-в-байт** против живого
`Model.sanitize`; int8 roundtrip sane. E2E на синтетике — оба режима
источника (bf16 и fp8) → 51/12/15, зелёный.

## 3. Доноры (§3.2) — вердикт: standalone `patches/glm52_mtp.py`

`alexcheema/mtp-speculative-decoding` (DeepSeek-V3, +1660):
лифтим композицию блока (enorm/hnorm/eh_proj + decoder-block + shared
embed/lm_head/final-norm), greedy-accept, откат через `trim_prompt_cache`.
Дефекты (не тащить): кормит **post-norm** hidden (`model()` возвращает
`norm(h)`, ds32:494 → double-norm); MTP-кэш без префилла и с offset 0
(RoPE-позиции врут на prompt_len); standalone-генератор мимо continuous
batching; dense-MLP-упрощение (наш слой 78 — MoE); веса патчем sanitize.

`david/speculative-mtp` (Qwen3.5, +5144): лифтим идеи — захват **pre-norm**
hidden обёрткой `inner.norm`; честный rejection sampling
(u < min(1, p/q), ресемпл из normalize(max(0,p−q)), bonus из p);
каркас batch-generator: гейт B==1 + прозрачный fallback + буфер принятых
токенов с выдачей по одному на шаг; стоп-обрезка внутри окна; chained-γ
(Фаза 4). Не тащим: GDN/кернелы, откат мутацией `offset -=` мимо `trim()`,
нестандартный `ratio**alpha`.

## 4. Точки интеграции (§3.3), проверено по дереву

- Декод-шаг: `mlx_lm.generate.GenerationBatch._step` (generate.py:1344),
  **уже перехвачен** `patches/opt_batch_gen._patched_step` (:180, установка
  :270 из bootstrap `apply_mlx_patches()`). Хук MTP — обёртка поверх
  установленного `_step` (сохранённый — fallback). Гейт: env on/shadow ∧
  family glm_moe_dsa ∧ `len(self.uids)==1` ∧ mtp.safetensors валиден;
  `buf.needs_topk` (logprobs) → warn-once + fallback (v1).
- Пайплайнинг: `_step` выдаёт токен предыдущей итерации
  (`_current=_next`, async_eval следующего) — MTP-цикл сохраняет инвариант:
  y = pending `_next_tokens`, verify L=1+k, новый `_next_tokens` =
  bonus/corrected, принятые — в буфер.
- Pre-norm hidden: обёртка `model.model.norm` (Дэвид-паттерн). TP `shard()`
  (ds32:603–659) шардит только внутренности слоёв; embed/norm/lm_head/
  indexer реплицированы → h и логиты реплицированы (факт 4 ТЗ ✓).
- Кэши: `CacheList.trim(n)` трогает оба слота (cache.py:824–827);
  shared-слои двигают cache[1] placeholder-апдейтом каждый forward
  (glm52_indexshare.py:436–448) → офсеты слот-0/слот-1 равны на всех 78
  слоях, откат единообразен. Смок в контейнере (реальные операторы):
  fwd6 vs fwd6→trim(2)→redo — **байт-в-байт**, оффсеты (6,6)→(4,4).
- Непатченные attention-инстансы (наш MTP-блок) уходят в
  `_exo_original_call` (glm52_indexshare.py:318) — IndexShare-контекст
  главной модели не задевается.
- W2-fingerprint собирает весь `EXO_GLM52_*` (prefill_config.py:42–51) →
  `EXO_GLM52_MTP*` в кросс-ранговом хэше автоматически (§9 ТЗ ✓).
- `mx.random.seed(seed)` + per-request sampler — batch_generate.py:185–194.

## 5. Дизайн-заметки для Фазы 1 (сверх ТЗ)

- D1. Блок = `DeepseekV32DecoderLayer(config, layer_idx=78)` пина: MoE-ветка
  выбирается автоматически (ds32:398–413), индексер в комплекте. Реплика на
  всех рангах, без шардинга (≈10.6GB int8/ранг).
- D2. MTP-кэш = свой `CacheList(KVCache, KVCache)`; lockstep trim с
  главными. v1 без префилла MTP-кэша по промпту → RoPE-позиции драфтера
  относительные. Если shadow-accept < гейта — первая мера: MTP-префилл
  по чанкам главного префилла (стоимость ~1/78 ≈ +1.3%) либо кастомный
  OffsetKVCache (виртуальный базовый offset). Решение по данным shadow.
- D3. M2: p и q для rejection sampling обязаны проходить **одинаковые**
  трансформации сэмплера (temp/top_p/min_p/top_k), иначе смещение.
- D4. Финиш/стоп внутри окна: перед `extract_cache` кэш триммится до
  фактически выданных токенов (иначе prefix-cache получит хвост-сироту).
  Тест в G2-A набор.
- D5. `buf.needs_topk` → fallback (v1); topk-конвейер под окно — не сейчас.
- D6. Загрузка side-car presence-driven: модуль квантован ⇔ есть
  `<name>.scales` в mtp.safetensors; конфиг-флаги не читаем.
- D7. Донорский `mtp_cache` без trim на reject у Алекса — при k=1 запись
  позиции валидна (считана от принятого (h,t)), но наша хореография всё
  равно lockstep-trim по ТЗ §5.5 — единый инвариант офсетов проще
  ассертить (`EXO_GLM52_MTP_VALIDATE=1`).

## 6. Дальше

1. Прогнать экстрактор по обоим источникам, sha256/счётчики в этот док
   (append) — закрытие 0-A артефактом.
2. Фаза 1: `patches/glm52_mtp.py` — блок + side-load + shadow-режим,
   телеметрия `[MTP_SHADOW]`; сутки на OpenCode-трафике; ГЕЙТ 1-A ≥60%.

---

## Итоги фаз 1–2 (2026-09-01) — что измерено, что решено

### Факты, закрывающие вопросы recon
- **Порядок eh_proj**: vLLM `deepseek_mtp.py` (обслуживает `glm_moe_dsa`):
  `eh_proj(cat([enorm(embeds), hnorm(hidden)]))` → дефолт `eh` верен; у донора
  `alexcheema` порядок перевёрнут (ещё один дефект донора). Вход MTP — **pre-norm** h
  (их же комментарий у compute_logits). vLLM форсит fp32-роутинг MoE для glm_moe_dsa —
  наш блок использует MoEGate пина, как и все 78 главных слоёв.
- **Декод-кэши — `BatchKVCache`** (граduация prompt→generation оборачивает plain
  KVCache), trim = `_idx/offset -= n`, offset — массив. Уточняет §2.1.
- **Ординал-1 аномалия**: первый запрос после загрузки численно ≠ последующим
  (h0sum 117.6 vs −82.4 при одинаковом префилле). Warm-слоты воспроизводимы.
  Протокол замеров: `--warmup 1`, сравниваем только warm-слоты.
- **fast-sync и нож**: под `--fast-sync` (штатный режим кластера) траектории при
  temp=0 плавают на ножевых токенах (маржи 0.125–0.375 = шаг bf16-сетки);
  VALIDATE=1 маскировал это пер-цикловым барьером (цена барьера ~1ms). Off под
  fast-sync подвержен так же. На живом корпусе (код+доки) ранние маржи жирные,
  ординал-1 и нож не воспроизводятся.
- **G2-A′**: побайтность on≡off кросс-L (L=2 verify против L=1 декода) на bf16
  недостижима и ни vLLM, ни SGLang её не обещают; гарантия — консистентность
  относительно собственного verify (enforced конструкцией + VALIDATE). Арбитр
  качества — G2-B digit-150k on vs off.

### Shadow (фаза 1)
Accept при temp=1.0 на бенч-генерации 0.83–0.93 (win256 до 0.94); paттерн
«низкий старт (пустой суффиксный MTP-кэш) → рост». Налог тени −1.7%.

### Три бага маршрутизации verify L=2 (все исправлены, 0006/0007/0011)
1. `cache_requires_dense_prefill` по наличию `left_padding` → dense по всему kv
   каждый цикл (0.69 t/s на 49k) + сдвиг accept (dense-h против sparse-мира).
2. `sparse_min_kv` гнал L=2 в dense-префилл-ветку при kv<12288 → micro-L
   (`_MICRO_DECODE_L=4`) всегда sparse при наличии topk.
3. tiled/streaming индексер для L=2 → монолит (экономии не дал, оставлен как
   корректный путь).

### Боевой M1 — матрица (warm, temp=0, корпус код+доки, VALIDATE=0)
| pp | off | on | прирост | accept | цикл |
|---|---|---|---|---|---|
| 4096 | 19.05 | 20.9–22.0 | +10–16% | 0.61–0.69 | 1.45–1.49 шага |
| 49152 | 18.55 | 22.8–23.1 | +23–24% | 0.82–0.85 | 1.48 |
| 147456 | 16.99 | 20.3–20.8 | +20–22% | 0.77–0.85 | 1.48 |

Профиль цикла (49k): build 8.2 / resolve 68.6 / post 2.3 ms — синхронный такт
(host резолвит m до всего остального; пин прячет ~14ms однотактным конвейером).
Accept на глубине **растёт** (продолжение документа предсказуемее свободного
ответа) → D2-прогрев MTP-кэша не нужен.

### M1.5 (чейнинг) — отрицательный результат, откачен
`mx.async_eval` допускает одну оценку в полёте (второй вызов блокируется до
завершения первой — измерено); slice-присваивание mx.array мутирует объект
in-place, выброшенный build-ahead граф остаётся в lineage кэша и считается.
Следствие: для k=1 синхронный такт — потолок структуры; цель ≥28 t/s на kv≥50k
берётся уменьшением работы на цикл — **k=2 цепные драфты (фаза 4)**, оценка
eff≈2.3–2.5 при accept₂≈0.6 → ~29–31 на 49k.

### Открытое
- G2-B digit-150k on/off (сиды 0/4/5) — арбитр качества.
- M2 (honest rejection sampling) — ускорение OpenCode (temp=1.0), сейчас его
  трафик под `on` идёт shadow-путём.
- Ординал-1 численность первого исполнения — тикет базового стека.

---

## Фазы 3–4 и аудиты (2026-09-02) — M2, конвенции hidden, итоговая матрица

### Конвенции hidden — закрыты замерами на GLM-5.3-8bit-idxbf16 (49k, корпус код+доки)
| что | вариант | a1 / a2 | вывод |
|---|---|---|---|
| целевой hidden (главная модель → MTP) | **pre**-final-norm | a1 0.816 | дефолт |
| | post-final-norm (конвенция vLLM) | a1 0.741 | хуже на 0.075 |
| рецикл в цепи (MTP-шаг → MTP-шаг, k≥2) | **post**-norm (vLLM PR #47448) | a2 0.560 | дефолт |
| | pre-norm | a2 0.413 | хуже на 0.15 |

Асимметрия измерена, а не выведена: `EXO_GLM52_MTP_HIDDEN=pre` (дефолт), `EXO_GLM52_MTP_RECYCLE=post` (дефолт).

### k=2 / k=3 — не окупаются на этом стеке
k=2 (temp 1.0, V=0): 49k 22.02 (+19%), 147k 18.06 (+6%) при a2≈0.56: второй MTP-шаг
(блок + lm_head) и verify L=3 стоят больше, чем приносят +0.45 токена/цикл; на глубине
цикл 126ms. k=3 при a3≈0.4 — ноль или минус. Рабочий конфиг — **k=1**.

### Аудиты №1/№2 — что подтвердилось и исправлено (0018–0028, 0030)
* P0.1 verify обходил `logits_processors` (bench EOS-ban видел только off) — «короткие
  траектории» были этим багом, не ножом; исправлено (прод-семантика `_patched_step`).
* P0.2 probe семплера при `min_p=0.05` принимал temp 0.7/1.0 за greedy → явная политика
  `ExoSampler`, fail-closed.
* P0.3 exactly-once реальный шаг, снапшот/rollback всех слотов, VALIDATE не глотается,
  `_trim_to_exact`; отказы внутри verify/коллективов не маскируются.
* P0.5 норма-обёртка — подкласс `nn.RMSNorm` (дерево параметров сохранено).
* P0.6/§7 side-car: манифест+sha+политика; rank-safe коллективы (все всегда входят с
  флагом; второй раунд readiness после сборки модуля; VALIDATE: локальные коды → одна
  коллективная операция → общий raise).
* P1.1 RoPE-решение индексера — и на MTP-блок. P1.2 seed-шаг на запрос. P1.4 micro-L
  только в `mtp_verify_context()`. P1.7/P1.8 экстрактор/корпус.
* §3 (аудит 2) merge-flush переигрывал уже принятый драфт из residual → искажал
  распределение; теперь принятый драфт = pending, без RNG. §5/§6 «призрачные» запросы
  после текстового стопа; cancel/close финализируют MTP.
* Монолитный индексер для verify возвращён **без** except-фолбэка, только в
  verify-контексте (аудит бил по фолбэку, не по монолиту): −18ms/цикл на 147k.
* Отклонено/отложено: P1.3 (accept на глубине растёт), харнесс L=1 vs L=2 (§2/§4),
  full-q proposal (§10), fast-lane без слияния при буфере (§12), strategy-hook рефакторинг.

### M2 — speculative sampling для temperature>0 (0027)
Целевое распределение восстанавливается функциями пина (top_p→min_p→top_k, затем /temp);
драфт детерминированный, accept с p(d), residual при отказе, bonus из target — one-hot
частный случай Leviathan et al., распределение текста сохраняется точно. Гейты: ядро на
20k розыгрышах (TV<0.02), сквозной тест второго токена (300 запросов), детерминизм при
равном seed, кросс-ранговая проверка семплированного токена в VALIDATE.

### Итоговая матрица (k=1, temperature 1.0, VALIDATE=0, корпус код+доки, warm)
| pp | off | MTP | прирост | цикл |
|---|---|---|---|---|
| 49152 | 18.55 | **22.11** | +19% | 80 ms (build 8.2 / resolve 69 / post 2.3) |
| 147456 | 16.99 | **20.45** | +20% | 88 ms |

accept a1 ≈ 0.80 на продолжении документа; на свободном чате ждать ниже.
Резервы: full-q proposal (драфт из распределения головы — для высокоэнтропийного чата),
частичная сортировка в top_p, M1.5-lite (спекулятивный драфт в цепочке verify).

---

## Прогрев головы промптом (D2, 0032–0038) — 2026-09-02, вечер

### Механика
exo префиллит `prompt[:-1]` чанками; после каждого чанка хук (`mtp_prefill_chunk`) берёт
захваченные hidden'ы чанка и прогоняет пары (h_i, t_{i+1}) через MTP-блок подчанками по 256
(carry последнего h между чанками). На seed-шаге пакет адоптируется структурно (хвост
токенов + позиция основного кэша = n; exo вставляет в генератор только два последних токена
промпта — полный список в `tokens[0]` не появляется). MTP-attention помечен как full-слой
IndexShare (`fast_attn=1`): без пометки блок шёл сырым путём пина — ~60ms на драфт над
кэшем в 49k и ~12с ingest'а. Ingest доказан байт-равным пути драфта (подчанки == один мах ==
по одной паре).

### Что измерено (49k, temperature 1.0, k=1)
| вариант | окно 1 | стационар | итог accept | t/s |
|---|---|---|---|---|
| холодная голова | 0.656 | ~0.80 | 0.796 | 22.1 |
| весь промпт в кэше | 0.766 | 0.66 | 0.673 | 20.5 |
| окно 2048 | 0.73–0.88 | 0.69–0.77 | 0.72–0.76 | 20.5–20.8 |
| **окно 2048 + сброс после 64 циклов** | **0.80–0.88** | **~0.80–0.84** | **0.816–0.829** | **22.0–22.3** |

Вывод: контекст промпта помогает голове только пока у неё нет собственного контекста
ответа; дальше вредит (любой объём). Дефолт: `EXO_GLM52_MTP_PREFILL=1`,
`EXO_GLM52_MTP_PREFILL_WINDOW=2048`, `EXO_GLM52_MTP_PREFILL_CYCLES=64`. Для коротких ответов
(чат) выигрыш первого окна весит больше, чем на 512-токенных бенчах.

### Инцидент: сегфолт S4 (17:26) и часы
Стек: `libthunderboltrdma tbt_post_recv ← jaccl RingImpl::all_reduce<bf16>` на comm-потоке
MLX в первом чанке префилла следующего запроса; главный поток — в `mlx_lm.generate_step`
(штатный `mx.eval` пина), MTP-кода в стеке нет. Гипотеза `mx.clear_cache` (LRU) — не
подтверждена логом. Класс бага — время жизни RDMA-регистраций в транспорте; churn аллокаций
повышает вероятность. Приняты 0037 (отставленный кэш живёт до конца запроса) и 0038
(никаких host-side eval внутри префилла — только `async_eval`). После: 3 прогона × 2 запроса
49k без падений. Второй крэш того же дня (14:01, `gpu::check_error`) — здоровье S4/fast-sync.

После ребута S4 часы уехали на −2.0 с; zenoh отбрасывает данные с |Δ|>500ms → control-plane
S4 глух. `timed` получал верный NTP, но **отвергал его как выброс** (`RejectATime, innv 1.97`)
и каждые 15 минут ставил свой прогноз (`from TMTimeSynthesizer, adjust −2.016`). Лечение:
сброс `/var/db/timed/com.apple.timed.plist` (после второго Reject модель пересобралась,
`adjust +1.947`), демон `com.local.ntp-sync` (sntp раз в час + при загрузке) на S1/S3/S4 как
страховка. Правило: после любого ребута брейн-ноды — сверка `date +%s.%N` по четырём нодам.

### M1.5-lite (0042/0043) — отрицательный результат, измерен
Спекулятивный драфт следующего цикла в цепочке verify. 49k, V=0, `sample`:
* 0042 (обход графа драфта в post): resolve 69→66.3, post 2.3→4.9 → цикл 79.4, 22.14 t/s (было 22.11).
* 0043 (verify+драфт одним async_eval до ожидания): post 2.3, но resolve 70.5 — `mx.eval(accs)`
  ждёт всю отправленную пачку, включая GPU-драфт → 21.65 t/s.
Вывод: MLX не даёт частичного ожидания внутри отправки; перекрытие драфта с host-фазами
недостижимо. Дефолт `EXO_GLM52_MTP_SPEC_DRAFT=0`, код под ручкой. Структура цикла на 49k:
build 8.3 (хост, сборка графа 78 слоёв) + resolve ~66 (GPU) + post 2.3 — пол стека.

### Чат (реальный трафик, temperature 1.0)
accept 0.55–0.62 (argmax-драфт); телеметрия `cf_fullq` 0.63–0.66 → `proposal=sample` даёт
измеренные 0.637. Σmin(p,q)≈0.65 — потолок качества головы на свободном тексте при
temperature 1.0: никакой способ драфта от этой головы выше не даст. Прогрев при хите
префикс-кэша (0040) работает (`prompt ingested: 58 pairs (base=7520)`), даёт +0.03 на
первом окне. Итог по чату ~7.5k контекста: 20.9 t/s.

---

## Итог M2-этапа (2026-09-03) — рабочий конфиг и цифры

Запуск: `EXO_GLM52_MTP=on` без единой ручки. Дефолты (enable-строка):
`k=1 hidden=pre recycle=post prompt_prefill=1 prefill_window=2048 prefill_cycles=64
proposal=sample cf=0 spec_draft=0 fast_attn=1 validate=0`. Канарейка `EXO_GLM52_MTP_VALIDATE=1`
— после обновлений пина/mlx.

| контекст | off | on | accept | прирост |
|---|---|---|---|---|
| 49k, корпус код+доки | 18.35 | **21.73** | 0.80 | **+18%** |
| 147k, корпус код+доки | 16.25 | **19.47** | 0.77 | **+20%** |
| чат ~7.5k (реальный трафик) | ~19.0 | **20.9** | 0.64 | **~+10%** |

temperature 1.0, один корпус для off/on, measured-слоты. Распределение текста сохраняется
точно (гейты RS для обоих proposal). Стабильность: 3×2 запроса 49k подряд без падений после
0037/0038; часы нод под контролем (`com.local.ntp-sync` + сброс состояния `timed`).

### Закрытые вопросы (не возвращаться без новых вводных)
* k=2 после 0030 (verify через монолитный индексер): 49k 22.35 (+2.9% к k=1), 147k 19.91 (+2.3%);
  a₂≈0.50–0.55. Для документов — небольшой плюс, для чата (a₁≈0.64) второй шаг не окупается.
  Дефолт k=1; k=3 не измерялся после 0030 (ожидаемо ниже k=2).
* Целевой hidden pre / рецикл post — измерено, асимметрия реальна.
* Прогрев промптом: только первые 64 цикла, потом свой контекст (весь промпт в кэше вредит).
* M1.5-lite — MLX не даёт частичного ожидания; net-zero/negative, выключено.
* Full-q proposal — +0.04 на чате, дефолт; потолок Σmin(p,q)≈0.65 — качество головы.
* Порог безубыточности accept ≈ 0.5 (C/S ≈ 1.48 на всех глубинах).

### Что могло бы дать ещё (по убыванию отдачи, все — крупные)
1. Дообучение MTP-головы под целевые тексты (единственный путь выше 0.65 на чате).
2. Два кандидата на первую позицию (мини-дерево) — требует multi-draft RS.
3. Автопредохранитель accept<0.5 (дешёвый, страховка от непредсказуемых текстов).

## Справочник: запуск и все ручки MTP

**Рабочий запуск (все дефолты измерены):**
```bash
EXO_GLM52_MTP=on EXO_PREFILL_STEP=1024 EXO_SSM_SNAPSHOT_EVERY=16 \
EXO_MODELS_DIRS="/Volumes/Models:/Volumes/Models2" .venv/bin/exo --fast-sync
```
`EXO_PREFILL_STEP` / `EXO_SSM_SNAPSHOT_EVERY` / `EXO_MODELS_DIRS` / `--fast-sync` — не про MTP, это
штатные ключи кластера. Всё, что ниже, — только MTP; ставить нужно лишь то, что отличается от дефолта.

| переменная | дефолт | значения | что делает |
|---|---|---|---|
| `EXO_GLM52_MTP` | `off` | `off` / `shadow` / `on` | `shadow` — только считает accept без ускорения; `on` — боевой режим |
| `EXO_GLM52_MTP_DRAFT_K` | `1` | 1–3 | драфтов за цикл; 2/3 не окупаются (измерено) |
| `EXO_GLM52_MTP_PROPOSAL` | `sample` | `sample` / `argmax` | драфт из распределения головы / её top-1; greedy-запросы всегда argmax |
| `EXO_GLM52_MTP_HIDDEN` | `pre` | `pre` / `post` | целевой hidden до/после финальной нормы (pre лучше на 0.075) |
| `EXO_GLM52_MTP_RECYCLE` | `post` | `post` / `pre` | hidden между шагами цепи при k≥2 |
| `EXO_GLM52_MTP_PREFILL` | `1` | 0/1 | прогрев головы промптом (и хвостом при хите префикс-кэша) |
| `EXO_GLM52_MTP_PREFILL_WINDOW` | `2048` | 0 = весь промпт | сколько последних пар промпта глотать |
| `EXO_GLM52_MTP_PREFILL_CYCLES` | `64` | 0 = не сбрасывать | после скольких циклов заменить контекст промпта своим |
| `EXO_GLM52_MTP_VALIDATE` | `0` | 0/1 | канарейка: кросс-ранговые проверки токенов/офсетов каждый цикл (~1–2% скорости) |
| `EXO_GLM52_MTP_PROF` | `0` | 0/1 | `[MTP_PROF]` build/resolve/post каждые 64 цикла |
| `EXO_GLM52_MTP_CF` | `0` | 0/1 | телеметрия «сколько принял бы другой proposal» |
| `EXO_GLM52_MTP_TRACE` | `0` | N | подробный трейс первых N циклов |
| `EXO_GLM52_MTP_SPEC_DRAFT` | `0` | 0/1 | M1.5-lite; на MLX не окупается |
| `EXO_GLM52_MTP_CONCAT` | `eh` | `eh` / `he` | порядок конкатенации в eh_proj (eh верен по vLLM) |
| `EXO_GLM52_MTP_WEIGHTS` | `auto` | путь | side-car `mtp.safetensors` (по умолчанию рядом с моделью) |

**Батчинг.** MTP работает только при одном активном запросе (B=1). Когда генератор объединяет
несколько запросов в батч, MTP на этот батч не применяется — идёт обычный decode; при слиянии
уже принятый, но не выданный драфт корректно сохраняется (merge-flush). Как только батч снова
сжимается до одного запроса, MTP возобновляется (seed-шаг → циклы). Ускорение B>1 требует
отдельного дизайна (per-row m, рваные префиксы, per-row rollback) и не сделано.

### Ширина (tree drafting) — измерено и закрыто арифметикой (2026-09-03)
Телеметрия `cf_top2`/`cf_top4` (масса главной модели на 2/4 лучших кандидатах головы = a₁
greedy-дерева на первой позиции), 49k, корпус: onehot 0.74, fullq 0.75, **top2 0.85, top4 0.91**.
Потолок высокий, но на MoE каждый лишний токен verify активирует свои экспертов — по замерам
k=1/k=2 это **+4–6 мс на токен** (на H100 при batch 1 почти бесплатно — отсюда рекомендация
SGLang «topk 4» для GLM, которая сюда не переносится):

| вариант | a₁ | eff | цикл | t/s |
|---|---|---|---|---|
| 1 кандидат | 0.75 | 1.75 | 80 | 21.9 |
| top-2 | 0.85 | 1.85 | 84–86 | 21.5–22.0 |
| top-4 | 0.91 | 1.91 | 92–98 | 19.5–20.8 |

Реализация потребовала бы RoPE-позиций по токену (главный attention + DSA-индексер), масок
«соседи не видят друг друга» в micro-L/индексере, переезда принятой строки кэша и multi-draft
RS — 3–4 сессии в самом хрупком месте форка за ±0 на чате. Не делаем.

**Общий вывод по платформе:** на MoE + TP4/Thunderbolt любая спекуляция, добавляющая токены
в verify (глубина k, ширина topk, их сочетания), платит ~5 мс/токен и упирается в одну стену.
Единственный рычаг выше — accept первой позиции при L=2, а он на потолке головы
(Σmin(p,q): 0.75 док / 0.65 чат). Дальше — только дообучение головы.

### Параллельные запросы
Префиллы в exo идут по очереди (без вытеснения). Пакеты прогрева паркуются (≤4) и подбираются
к своему запросу по позиции кэша и хвосту токенов (0049). При объединении запросов в батч MTP
выключается на время перекрытия (`[MTP_SUMMARY] (batch-merge)`), генерация продолжается
обычным декодом; после сжатия батча до одного запроса MTP возобновляется.
